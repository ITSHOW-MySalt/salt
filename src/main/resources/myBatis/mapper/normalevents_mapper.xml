<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.salt.dao.NormalEventsDAO">
    <!--    namespace: dao 가 있는 풀패키지-->

    <select id="selectById" parameterType="int" resultType="com.example.salt.dto.NormalEventsDTO">
        SELECT * FROM normalevents_tb WHERE id = #{id}
    </select>
</mapper>